#include <stdio.h>
#include <cs50.h>

int main(void)
{
    long card_number = get_long("Number: ");
    if (card_number)
    {
        printf("%long", card_number);
    }
    else if (card_number)
    {
    }
    else
    {
    }
}